/**
 * GetException handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: exceptions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getException } from "@erasuremesh/api-server/domains/exceptions/handlers";
import type { ExceptionsDDDDependencies } from "@erasuremesh/api-server/domains/exceptions";
import { createException as createExceptionEntity } from "@erasuremesh/factories/exceptions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@erasuremesh/tests/shared";
import { EXCEPTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetException", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ExceptionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ExceptionsDDDDependencies>(
      EXCEPTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createExceptionEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetException, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { exceptionId: entity.id };

    await getException(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetException.execute).toHaveBeenCalledWith(expect.objectContaining({ exceptionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { exceptionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetException, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getException(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
