/**
 * TestProcessorNodeAckEndpoint handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: processor-graph
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { testProcessorNodeAckEndpoint } from "@erasuremesh/api-server/domains/processor-graph/handlers";
import type { ProcessorGraphDDDDependencies } from "@erasuremesh/api-server/domains/processor-graph";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@erasuremesh/tests/shared";
import { PROCESSORGRAPH_USE_CASES } from "../__factories__/use-cases.js";

describe("TestProcessorNodeAckEndpoint", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ProcessorGraphDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ProcessorGraphDDDDependencies>(
      PROCESSORGRAPH_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeTestProcessorNodeAckEndpoint, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { nodeId: entity.id };

    await testProcessorNodeAckEndpoint(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeTestProcessorNodeAckEndpoint.execute).toHaveBeenCalledWith(expect.objectContaining({ nodeId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { nodeId: nonExistentId };
    
    vi.spyOn(mockDeps.executeTestProcessorNodeAckEndpoint, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      testProcessorNodeAckEndpoint(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
