/**
 * Dispatch Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: erasure-orders
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { DispatchEntity } from "@erasuremesh/core/erasure-orders/models/index.js";
import { erasureOrdersSchemas } from "@erasuremesh/core/erasure-orders";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "ero";

export function createDispatch(overrides: Partial<DispatchEntity> = {}) {
  const raw = {
    id: overrides.id ?? `eor_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    channel: overrides.channel ?? "written",
    derivedArtefactsPending: overrides.derivedArtefactsPending ?? faker.number.int(),
    dueAt: overrides.dueAt ?? new Date().toISOString(),
    status: overrides.status ?? "open",
    subjectRef: overrides.subjectRef ?? faker.lorem.word(),
    ...overrides,
  };

  return erasureOrdersSchemas.ErasureOrder.parse(raw);
}

export function dispatchBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createDispatch(overrides));
}
