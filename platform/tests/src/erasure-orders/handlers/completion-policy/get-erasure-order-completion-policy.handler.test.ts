/**
 * GetErasureOrderCompletionPolicy handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: erasure-orders
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getErasureOrderCompletionPolicy } from "@erasuremesh/api-server/domains/erasure-orders/handlers";
import type { ErasureOrdersDDDDependencies } from "@erasuremesh/api-server/domains/erasure-orders";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@erasuremesh/tests/shared";
import { ERASUREORDERS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetErasureOrderCompletionPolicy", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ErasureOrdersDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ErasureOrdersDDDDependencies>(
      ERASUREORDERS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetErasureOrderCompletionPolicy, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { orderId: entity.id };

    await getErasureOrderCompletionPolicy(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetErasureOrderCompletionPolicy.execute).toHaveBeenCalledWith(expect.objectContaining({ orderId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { orderId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetErasureOrderCompletionPolicy, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getErasureOrderCompletionPolicy(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
