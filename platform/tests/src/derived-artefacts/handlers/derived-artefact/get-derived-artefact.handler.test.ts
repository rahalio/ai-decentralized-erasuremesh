/**
 * GetDerivedArtefact handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: derived-artefacts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getDerivedArtefact } from "@erasuremesh/api-server/domains/derived-artefacts/handlers";
import type { DerivedArtefactsDDDDependencies } from "@erasuremesh/api-server/domains/derived-artefacts";
import { createDerivedArtefact as createDerivedArtefactEntity } from "@erasuremesh/factories/derived-artefacts";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@erasuremesh/tests/shared";
import { DERIVEDARTEFACTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetDerivedArtefact", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DerivedArtefactsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DerivedArtefactsDDDDependencies>(
      DERIVEDARTEFACTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createDerivedArtefactEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetDerivedArtefact, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { artefactId: entity.id };

    await getDerivedArtefact(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetDerivedArtefact.execute).toHaveBeenCalledWith(expect.objectContaining({ artefactId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { artefactId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetDerivedArtefact, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getDerivedArtefact(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
