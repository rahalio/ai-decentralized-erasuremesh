/**
 * VerifyEvidencePackSeal handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evidence-packs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { verifyEvidencePackSeal } from "@erasuremesh/api-server/domains/evidence-packs/handlers";
import type { EvidencePacksDDDDependencies } from "@erasuremesh/api-server/domains/evidence-packs";
import { createCreateVerifySealRequest } from "@erasuremesh/factories/evidence-packs";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@erasuremesh/tests/shared";
import { EVIDENCEPACKS_USE_CASES } from "../__factories__/use-cases.js";

describe("VerifyEvidencePackSeal", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: EvidencePacksDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<EvidencePacksDDDDependencies>(
      EVIDENCEPACKS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateVerifySealRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeVerifyEvidencePackSeal, "execute").mockResolvedValue(useCaseResult);

    await verifyEvidencePackSeal(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeVerifyEvidencePackSeal.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
