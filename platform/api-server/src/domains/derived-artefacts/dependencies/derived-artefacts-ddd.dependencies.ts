/**
 * DerivedArtefacts DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: derived-artefacts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * DerivedArtefacts Domain Module - Composition root
 *
 * DDD: Composition root for derived-artefacts domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  BlockCohortRefreshRepositoryAdapter,
  DerivedArtefactRepositoryAdapter,
  ExemptRepositoryAdapter,
  PurgeRepositoryAdapter,
} from "@erasuremesh/adapters/derived-artefacts";
import { getIdGeneratorService } from "@erasuremesh/adapters";
import type { AdapterDynamoDBClient } from "@erasuremesh/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteBlockDerivedArtefactCohortRefresh,
  ExecuteExemptDerivedArtefact,
  ExecuteGetDerivedArtefact,
  ExecuteListDerivedArtefacts,
  ExecutePurgeDerivedArtefact,
  ExecuteRegisterDerivedArtefact,
} from "@erasuremesh/services/derived-artefacts/usecases";
// Ports (interfaces)
import type {
  BlockCohortRefreshRepository,
  DerivedArtefactRepository,
  ExemptRepository,
  PurgeRepository,
} from "@erasuremesh/services/derived-artefacts/ports";

export interface DerivedArtefactsDomainModule {
  repos: {
    blockCohortRefreshes: BlockCohortRefreshRepository;
    derivedArtefacts: DerivedArtefactRepository;
    exempts: ExemptRepository;
    purges: PurgeRepository;
  };
  useCases: {
    blockCohortRefreshes: {
      get: ExecuteBlockDerivedArtefactCohortRefresh;
    };
    derivedArtefacts: {
      create: ExecuteRegisterDerivedArtefact;
      get: ExecuteGetDerivedArtefact;
      list: ExecuteListDerivedArtefacts;
    };
    exempts: {
      get: ExecuteExemptDerivedArtefact;
    };
    purges: {
      get: ExecutePurgeDerivedArtefact;
    };
  };
}

export function buildDerivedArtefactsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): DerivedArtefactsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      blockCohortRefreshes?: BlockCohortRefreshRepository;
      derivedArtefacts?: DerivedArtefactRepository;
      exempts?: ExemptRepository;
      purges?: PurgeRepository;
    } = {};

    obj.blockCohortRefreshes = new BlockCohortRefreshRepositoryAdapter(dynamoClient);
    obj.derivedArtefacts = new DerivedArtefactRepositoryAdapter(dynamoClient);
    obj.exempts = new ExemptRepositoryAdapter(dynamoClient);
    obj.purges = new PurgeRepositoryAdapter(dynamoClient);
    return {
      blockCohortRefreshes: obj.blockCohortRefreshes!,
      derivedArtefacts: obj.derivedArtefacts!,
      exempts: obj.exempts!,
      purges: obj.purges!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    blockCohortRefreshes: {
      get: new ExecuteBlockDerivedArtefactCohortRefresh(executionContext, idGenerator, repos.blockCohortRefreshes),
    },
    derivedArtefacts: {
      create: new ExecuteRegisterDerivedArtefact(executionContext, idGenerator, repos.derivedArtefacts),
      get: new ExecuteGetDerivedArtefact(executionContext, idGenerator, repos.derivedArtefacts),
      list: new ExecuteListDerivedArtefacts(executionContext, idGenerator, repos.derivedArtefacts),
    },
    exempts: {
      get: new ExecuteExemptDerivedArtefact(executionContext, idGenerator, repos.exempts),
    },
    purges: {
      get: new ExecutePurgeDerivedArtefact(executionContext, idGenerator, repos.purges),
    },
  };
  return { repos, useCases: useCases as DerivedArtefactsDomainModule["useCases"] };
}
