/**
 * ExecuteGetProcessorNode - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: processor-graph
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetProcessorNode Use Case
 *
 * DDD: Application use case for get ProcessorNode.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetProcessorNodeInput, GetProcessorNodeOutput } from "../dto/processor-node.dto";
import type { ExecutionContextService, IdGeneratorService } from "@erasuremesh/services/_shared/index.js";
import type { ProcessorNodeRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetProcessorNode {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly processorNode: ProcessorNodeRepository
  ) {}

  async execute(input: GetProcessorNodeInput): Promise<GetProcessorNodeOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.pgrId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).nodeId) {
      throw new ValidationError("nodeId is required");
    }
    const nodeId = (input as any).nodeId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.processorNode.getProcessorNode({ nodeId: nodeId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${nodeId}`);
    }

    return entity as GetProcessorNodeOutput;
  }
}
