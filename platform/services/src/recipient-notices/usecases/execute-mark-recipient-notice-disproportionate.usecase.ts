/**
 * ExecuteMarkRecipientNoticeDisproportionate - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: recipient-notices
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteMarkRecipientNoticeDisproportionate Use Case
 *
 * DDD: Application use case for get DisproportionateEffort.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { MarkRecipientNoticeDisproportionateInput, MarkRecipientNoticeDisproportionateOutput } from "../dto/disproportionate-effort.dto";
import type { ExecutionContextService, IdGeneratorService } from "@erasuremesh/services/_shared/index.js";
import type { DisproportionateEffortRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteMarkRecipientNoticeDisproportionate {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly disproportionateEffort: DisproportionateEffortRepository
  ) {}

  async execute(input: MarkRecipientNoticeDisproportionateInput): Promise<MarkRecipientNoticeDisproportionateOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.rcnId();

    // All field names extracted from OpenAPI spec: rationale
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).noticeId) {
      throw new ValidationError("noticeId is required");
    }
    const noticeId = (input as any).noticeId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.disproportionateEffort.markRecipientNoticeDisproportionate({ noticeId: noticeId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${noticeId}`);
    }

    return entity as MarkRecipientNoticeDisproportionateOutput;
  }
}
