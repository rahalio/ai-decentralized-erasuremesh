/**
 * EvidencePacks DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: evidence-packs
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * EvidencePacks Domain Module - Composition root
 *
 * DDD: Composition root for evidence-packs domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  EvidencePackRepositoryAdapter,
  SealRepositoryAdapter,
  ShareRepositoryAdapter,
  VerifySealRepositoryAdapter,
} from "@erasuremesh/adapters/evidence-packs";
import { getIdGeneratorService } from "@erasuremesh/adapters";
import type { AdapterDynamoDBClient } from "@erasuremesh/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAssembleEvidencePack,
  ExecuteGetEvidencePack,
  ExecuteListEvidencePacks,
  ExecuteSealEvidencePack,
} from "@erasuremesh/services/evidence-packs/usecases";
// Ports (interfaces)
import type {
  EvidencePackRepository,
  SealRepository,
  ShareRepository,
  VerifySealRepository,
} from "@erasuremesh/services/evidence-packs/ports";

export interface EvidencePacksDomainModule {
  repos: {
    evidencePacks: EvidencePackRepository;
    seals: SealRepository;
    shares: ShareRepository;
    verifySeals: VerifySealRepository;
  };
  useCases: {
    evidencePacks: {
      get: ExecuteAssembleEvidencePack;
      list: ExecuteListEvidencePacks;
    };
    seals: {
      get: ExecuteSealEvidencePack;
    };
  };
}

export function buildEvidencePacksDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): EvidencePacksDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      evidencePacks?: EvidencePackRepository;
      seals?: SealRepository;
      shares?: ShareRepository;
      verifySeals?: VerifySealRepository;
    } = {};

    obj.evidencePacks = new EvidencePackRepositoryAdapter(dynamoClient);
    obj.seals = new SealRepositoryAdapter(dynamoClient);
    obj.shares = new ShareRepositoryAdapter(dynamoClient);
    obj.verifySeals = new VerifySealRepositoryAdapter(dynamoClient);
    return {
      evidencePacks: obj.evidencePacks!,
      seals: obj.seals!,
      shares: obj.shares!,
      verifySeals: obj.verifySeals!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    evidencePacks: {
      get: new ExecuteAssembleEvidencePack(executionContext, idGenerator, repos.evidencePacks),
      list: new ExecuteListEvidencePacks(executionContext, idGenerator, repos.evidencePacks),
    },
    seals: {
      get: new ExecuteSealEvidencePack(executionContext, idGenerator, repos.seals),
    },
  };
  return { repos, useCases: useCases as EvidencePacksDomainModule["useCases"] };
}
