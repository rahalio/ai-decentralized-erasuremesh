/**
 * ExecuteLinkProcessorEdge - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: processor-graph
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteLinkProcessorEdge Use Case
 *
 * DDD: Application use case for get Edge.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { LinkProcessorEdgeInput, LinkProcessorEdgeOutput } from "../dto/edge.dto";
import type { ExecutionContextService, IdGeneratorService } from "@erasuremesh/services/_shared/index.js";
import type { EdgeRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteLinkProcessorEdge {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly edge: EdgeRepository
  ) {}

  async execute(input: LinkProcessorEdgeInput): Promise<LinkProcessorEdgeOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.pgrId();

    // All field names extracted from OpenAPI spec: contractualBasis, contractualRole, fromNodeId, toNodeId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).fromNodeId) {
      throw new ValidationError("fromNodeId is required");
    }
    const fromnodeId = (input as any).fromNodeId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.edge.linkProcessorEdge({ fromNodeId: fromnodeId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${fromnodeId}`);
    }

    return entity as LinkProcessorEdgeOutput;
  }
}
