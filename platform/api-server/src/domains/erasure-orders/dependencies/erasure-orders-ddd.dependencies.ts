/**
 * ErasureOrders DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: erasure-orders
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ErasureOrders Domain Module - Composition root
 *
 * DDD: Composition root for erasure-orders domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AcknowledgementRepositoryAdapter,
  CompleteRepositoryAdapter,
  CompletionPolicyRepositoryAdapter,
  DispatchRepositoryAdapter,
  ErasureOrderRepositoryAdapter,
  SlaExtensionRepositoryAdapter,
  TaskRepositoryAdapter,
} from "@erasuremesh/adapters/erasure-orders";
import { getIdGeneratorService } from "@erasuremesh/adapters";
import type { AdapterDynamoDBClient } from "@erasuremesh/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteAcknowledgeErasureTask,
  ExecuteCompleteErasureOrder,
  ExecuteCreateErasureOrder,
  ExecuteDispatchErasureOrder,
  ExecuteExtendErasureOrderSla,
  ExecuteGetErasureOrder,
  ExecuteListErasureOrderTasks,
  ExecuteListErasureOrders,
} from "@erasuremesh/services/erasure-orders/usecases";
// Ports (interfaces)
import type {
  AcknowledgementRepository,
  CompleteRepository,
  CompletionPolicyRepository,
  DispatchRepository,
  ErasureOrderRepository,
  SlaExtensionRepository,
  TaskRepository,
} from "@erasuremesh/services/erasure-orders/ports";

export interface ErasureOrdersDomainModule {
  repos: {
    acknowledgements: AcknowledgementRepository;
    completes: CompleteRepository;
    completionPolicies: CompletionPolicyRepository;
    dispatches: DispatchRepository;
    erasureOrders: ErasureOrderRepository;
    slaExtensions: SlaExtensionRepository;
    tasks: TaskRepository;
  };
  useCases: {
    acknowledgements: {
      get: ExecuteAcknowledgeErasureTask;
    };
    completes: {
      create: ExecuteCompleteErasureOrder;
    };
    dispatches: {
      get: ExecuteDispatchErasureOrder;
    };
    erasureOrders: {
      create: ExecuteCreateErasureOrder;
      get: ExecuteGetErasureOrder;
      list: ExecuteListErasureOrders;
    };
    slaExtensions: {
      get: ExecuteExtendErasureOrderSla;
    };
    tasks: {
      list: ExecuteListErasureOrderTasks;
    };
  };
}

export function buildErasureOrdersDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): ErasureOrdersDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      acknowledgements?: AcknowledgementRepository;
      completes?: CompleteRepository;
      completionPolicies?: CompletionPolicyRepository;
      dispatches?: DispatchRepository;
      erasureOrders?: ErasureOrderRepository;
      slaExtensions?: SlaExtensionRepository;
      tasks?: TaskRepository;
    } = {};

    obj.acknowledgements = new AcknowledgementRepositoryAdapter(dynamoClient);
    obj.completes = new CompleteRepositoryAdapter(dynamoClient);
    obj.completionPolicies = new CompletionPolicyRepositoryAdapter(dynamoClient);
    obj.dispatches = new DispatchRepositoryAdapter(dynamoClient);
    obj.erasureOrders = new ErasureOrderRepositoryAdapter(dynamoClient);
    obj.slaExtensions = new SlaExtensionRepositoryAdapter(dynamoClient);
    obj.tasks = new TaskRepositoryAdapter(dynamoClient);
    return {
      acknowledgements: obj.acknowledgements!,
      completes: obj.completes!,
      completionPolicies: obj.completionPolicies!,
      dispatches: obj.dispatches!,
      erasureOrders: obj.erasureOrders!,
      slaExtensions: obj.slaExtensions!,
      tasks: obj.tasks!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    acknowledgements: {
      get: new ExecuteAcknowledgeErasureTask(executionContext, idGenerator, repos.acknowledgements),
    },
    completes: {
      create: new ExecuteCompleteErasureOrder(executionContext, idGenerator, repos.completes),
    },
    dispatches: {
      get: new ExecuteDispatchErasureOrder(executionContext, idGenerator, repos.dispatches),
    },
    erasureOrders: {
      create: new ExecuteCreateErasureOrder(executionContext, idGenerator, repos.erasureOrders),
      get: new ExecuteGetErasureOrder(executionContext, idGenerator, repos.erasureOrders),
      list: new ExecuteListErasureOrders(executionContext, idGenerator, repos.erasureOrders),
    },
    slaExtensions: {
      get: new ExecuteExtendErasureOrderSla(executionContext, idGenerator, repos.slaExtensions),
    },
    tasks: {
      list: new ExecuteListErasureOrderTasks(executionContext, idGenerator, repos.tasks),
    },
  };
  return { repos, useCases: useCases as ErasureOrdersDomainModule["useCases"] };
}
